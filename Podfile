# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ClockWidget' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ClockWidget

end

target 'KHClock' do
  use_frameworks!
  pod "Shimmer"
  pod "Shift"
end

target 'Divergence Clock' do
    use_frameworks!
end

target 'Divergence Date' do
    use_frameworks!
end
